rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tokens/{document=**} {
      allow read, write: if true; // Extremely permissive for now, secure later
    }
  }
}
